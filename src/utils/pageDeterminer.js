// helpers.js

export function isFrontPage() {
    // Your logic for determining if it's the front page
}

export function isPage(pageName) {
    // Your logic for determining if it's a specific page
}

export function homeUrl() {
    // Your logic for determining the home URL
}